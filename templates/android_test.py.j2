"""
{{ test_class_name }} — Android/Appium Test
Project: {{ project_name }}
Screen: {{ screen_class_name }}
Run: pytest projects/{{ project_name }}/tests/ -m android -v
"""
import pytest
import allure

from core.base_android_test import BaseAndroidTest
from projects.{{ project_name }}.screens.{{ screen_module }} import {{ screen_class_name }}


@allure.suite("Android — {{ project_name | title }}")
@allure.feature("{{ feature_name }}")
@pytest.mark.android
class {{ test_class_name }}(BaseAndroidTest):
    """{{ docstring }}"""

{% for method in test_methods %}
    @allure.title("{{ method.title }}")
    @allure.severity(allure.severity_level.{{ method.severity | upper }})
    @pytest.mark.{{ method.marker }}
    def {{ method.name }}(self):
        """{{ method.docstring }}"""
        screen = {{ screen_class_name }}(self.driver)
        # TODO: implement test logic
        # Example:
        # assert screen.is_displayed(), "Screen should be visible"
        raise NotImplementedError("Implement this test")

{% endfor %}
